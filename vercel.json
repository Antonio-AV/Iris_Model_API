{
    "version": 2,
    "builds": [
        {
            "src": "api_modelo.py",
            "use": "@vercel/python",
            "config": {
                "installCommand": "echo 'Listing current directory:' && ls -al && echo 'Installing dependencies:' && pip install -r requirements.txt --no-cache-dir --verbose"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api_modelo.py"
        }
    ]
}
